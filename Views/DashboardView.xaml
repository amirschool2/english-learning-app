<UserControl x:Class="EnglishLearningApp.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="White" Margin="10">
    <ScrollViewer>
        <StackPanel Margin="20">
            <TextBlock Text="Dashboard" FontSize="28" FontWeight="Bold" Foreground="#2C3E50" Margin="0,0,0,20"/>
            
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                
                <!-- Card 1: Total Words -->
                <Border Grid.Column="0" Background="#3498DB" CornerRadius="8" Padding="20" Margin="0,0,10,0">
                    <StackPanel>
                        <TextBlock Text="Total Words" Foreground="White" FontSize="14" FontWeight="Bold"/>
                        <TextBlock Text="{Binding TotalWords}" Foreground="White" FontSize="36" FontWeight="Bold" Margin="0,10,0,0"/>
                    </StackPanel>
                </Border>

                <!-- Card 2: Mastered -->
                <Border Grid.Column="1" Background="#2ECC71" CornerRadius="8" Padding="20" Margin="5,0">
                    <StackPanel>
                        <TextBlock Text="Mastered" Foreground="White" FontSize="14" FontWeight="Bold"/>
                        <TextBlock Text="{Binding MasterWords}" Foreground="White" FontSize="36" FontWeight="Bold" Margin="0,10,0,0"/>
                    </StackPanel>
                </Border>

                <!-- Card 3: Due for Review -->
                <Border Grid.Column="2" Background="#E74C3C" CornerRadius="8" Padding="20" Margin="10,0,0,0">
                    <StackPanel>
                        <TextBlock Text="Due for Review" Foreground="White" FontSize="14" FontWeight="Bold"/>
                        <TextBlock Text="{Binding ReviewDue}" Foreground="White" FontSize="36" FontWeight="Bold" Margin="0,10,0,0"/>
                    </StackPanel>
                </Border>
            </Grid>

            <Grid Margin="0,30,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Daily Streak -->
                <Border Grid.Column="0" Background="#F39C12" CornerRadius="8" Padding="20" Margin="0,0,10,0">
                    <StackPanel>
                        <TextBlock Text="Daily Streak" Foreground="White" FontSize="14" FontWeight="Bold"/>
                        <TextBlock Text="{Binding DailyStreak}" Foreground="White" FontSize="32" FontWeight="Bold" Margin="0,10,0,0"/>
                        <TextBlock Text="days" Foreground="White" FontSize="12" Margin="0,5,0,0"/>
                    </StackPanel>
                </Border>

                <!-- Average Quiz Score -->
                <Border Grid.Column="1" Background="#9B59B6" CornerRadius="8" Padding="20" Margin="10,0,0,0">
                    <StackPanel>
                        <TextBlock Text="Average Quiz Score" Foreground="White" FontSize="14" FontWeight="Bold"/>
                        <TextBlock Text="{Binding AverageQuizScore, StringFormat='{0:F1}%'}" Foreground="White" FontSize="32" FontWeight="Bold" Margin="0,10,0,0"/>
                    </StackPanel>
                </Border>
            </Grid>

            <TextBlock Text="Quick Tips" FontSize="18" FontWeight="Bold" Foreground="#2C3E50" Margin="0,40,0,15"/>
            <TextBlock Text="• Practice regularly to maintain your daily streak" TextWrapping="Wrap" Foreground="#555" Margin="0,5"/>
            <TextBlock Text="• Use flashcards to review words you're struggling with" TextWrapping="Wrap" Foreground="#555" Margin="0,5"/>
            <TextBlock Text="• Take quizzes to test your knowledge" TextWrapping="Wrap" Foreground="#555" Margin="0,5"/>
            <TextBlock Text="• Listen to pronunciation to improve speaking skills" TextWrapping="Wrap" Foreground="#555" Margin="0,5"/>
        </StackPanel>
    </ScrollViewer>
</UserControl>
