<UserControl x:Class="EnglishLearningApp.Views.ProgressReportView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="White" Margin="10">
    <ScrollViewer>
        <StackPanel Margin="20">
            <TextBlock Text="Progress Report" FontSize="28" FontWeight="Bold" Foreground="#2C3E50" Margin="0,0,0,20"/>

            <!-- Statistics Grid -->
            <Grid Margin="0,0,0,30">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Total Words -->
                <Border Grid.Column="0" Background="#3498DB" CornerRadius="8" Padding="20" Margin="0,0,10,0">
                    <StackPanel>
                        <TextBlock Text="Total Words" Foreground="White" FontSize="12" FontWeight="Bold"/>
                        <TextBlock Text="{Binding TotalWords}" Foreground="White" FontSize="32" FontWeight="Bold" Margin="0,5,0,0"/>
                    </StackPanel>
                </Border>

                <!-- Mastered Words -->
                <Border Grid.Column="1" Background="#2ECC71" CornerRadius="8" Padding="20" Margin="5,0">
                    <StackPanel>
                        <TextBlock Text="Mastered" Foreground="White" FontSize="12" FontWeight="Bold"/>
                        <TextBlock Text="{Binding MasterWords}" Foreground="White" FontSize="32" FontWeight="Bold" Margin="0,5,0,0"/>
                    </StackPanel>
                </Border>

                <!-- Due for Review -->
                <Border Grid.Column="2" Background="#E74C3C" CornerRadius="8" Padding="20" Margin="10,0,0,0">
                    <StackPanel>
                        <TextBlock Text="Due for Review" Foreground="White" FontSize="12" FontWeight="Bold"/>
                        <TextBlock Text="{Binding ReviewDue}" Foreground="White" FontSize="32" FontWeight="Bold" Margin="0,5,0,0"/>
                    </StackPanel>
                </Border>
            </Grid>

            <!-- Additional Stats -->
            <Grid Margin="0,0,0,30">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Daily Streak -->
                <Border Grid.Column="0" Background="#F39C12" CornerRadius="8" Padding="20" Margin="0,0,10,0">
                    <StackPanel>
                        <TextBlock Text="Daily Streak" Foreground="White" FontSize="12" FontWeight="Bold"/>
                        <TextBlock Text="{Binding DailyStreak}" Foreground="White" FontSize="32" FontWeight="Bold" Margin="0,5,0,0"/>
                        <TextBlock Text="days" Foreground="#FFF8DC" FontSize="10" Margin="0,2,0,0"/>
                    </StackPanel>
                </Border>

                <!-- Total Quizzes -->
                <Border Grid.Column="1" Background="#9B59B6" CornerRadius="8" Padding="20" Margin="5,0">
                    <StackPanel>
                        <TextBlock Text="Quizzes Taken" Foreground="White" FontSize="12" FontWeight="Bold"/>
                        <TextBlock Text="{Binding TotalQuizzes}" Foreground="White" FontSize="32" FontWeight="Bold" Margin="0,5,0,0"/>
                    </StackPanel>
                </Border>

                <!-- Average Score -->
                <Border Grid.Column="2" Background="#1ABC9C" CornerRadius="8" Padding="20" Margin="10,0,0,0">
                    <StackPanel>
                        <TextBlock Text="Avg Quiz Score" Foreground="White" FontSize="12" FontWeight="Bold"/>
                        <TextBlock Text="{Binding AverageScore, StringFormat='{0:F1}%'}" Foreground="White" FontSize="32" FontWeight="Bold" Margin="0,5,0,0"/>
                    </StackPanel>
                </Border>
            </Grid>

            <!-- Recent Quizzes -->
            <TextBlock Text="Recent Quizzes" FontSize="18" FontWeight="Bold" Foreground="#2C3E50" Margin="0,0,0,15"/>

            <DataGrid ItemsSource="{Binding RecentQuizzes}" AutoGenerateColumns="False" 
                      Background="#F9F9F9" Foreground="#333" GridLinesVisibility="None">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Date" Binding="{Binding Date}" Width="150"/>
                    <DataGridTextColumn Header="Type" Binding="{Binding Type}" Width="100"/>
                    <DataGridTextColumn Header="Score" Binding="{Binding Score}" Width="100"/>
                    <DataGridTextColumn Header="Percentage" Binding="{Binding Percentage, StringFormat='{0}%'}" Width="100"/>
                </DataGrid.Columns>
            </DataGrid>
        </StackPanel>
    </ScrollViewer>
</UserControl>
